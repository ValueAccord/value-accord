import { PageShell } from "@/components/site-chrome";
const steps=[['01','Intent','The merchant declares the exact amount, currency, expiry, and acceptance conditions.'],['02','Quote','Mock providers return comparable USD→EUR offers with fee, speed, and reliability.'],['03','Route','The router selects the lowest viable total cost, with deterministic tie-breaks.'],['04','Hold','The buyer’s test balance is conditionally held while authorization is verified.'],['05','Packet','A signed, expiring, single-use Value Packet binds the intent to the chosen route.'],['06','Receipt','The merchant receives exact test EUR and both parties receive durable proof.']];
export default function How(){return <PageShell eyebrow="HOW IT WORKS" title="Coordination, not custody." intro="Value Accord defines messages and proofs. Providers remain responsible for regulated execution in any future real-world implementation."><div className="step-list">{steps.map(([n,t,d])=><article key={n}><span>{n}</span><h2>{t}</h2><p>{d}</p></article>)}</div><aside className="callout"><strong>Current prototype boundary</strong><p>All balances, providers, quotes, holds, settlements, refunds, and disputes are simulated in memory. No real money or payment credentials enter the system.</p></aside></PageShell>}
