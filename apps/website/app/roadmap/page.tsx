import { PageShell } from "@/components/site-chrome";
const phases=[['NOW','Sandbox foundation','Deterministic routing, virtual balances, Ed25519 packets, receipts, refunds, disputes.'],['NEXT','Protocol hardening','Schema validation, conformance fixtures, replay registry, provider failure simulation.'],['LATER','Multi-party pilots','Independent implementations, interoperability test events, governance formation.'],['GATED','Regulated experimentation','Only with qualified partners, jurisdiction review, security audit, and explicit approvals.']];
export default function Roadmap(){return <PageShell eyebrow="ROADMAP" title="Evidence before expansion." intro="The project advances by proving small claims. No promise to replace existing networks; no real-money pilot before the controls and partnerships exist."><div className="timeline">{phases.map(([when,title,body],i)=><article key={when}><div><span>{String(i+1).padStart(2,'0')}</span><b>{when}</b></div><h2>{title}</h2><p>{body}</p></article>)}</div></PageShell>}
